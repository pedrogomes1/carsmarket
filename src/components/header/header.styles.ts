import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 60,
    marginRight: 20,
    marginLeft: 20,
  },
  containerProfile: {
    flexDirection: 'column',
    marginLeft: 20,
  },
  text: {
    color: '#fff',
  },
  username: {
    fontSize: 16,
  },
})
