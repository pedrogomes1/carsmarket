import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 60,
    marginHorizontal: 20,
  },
  containerProfile: {
    flexDirection: 'column',
    marginLeft: 20,
  },
  username: {
    fontSize: 16,
  },
  logoutButton: {
    marginLeft: 'auto',
  },
})
