import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    color: '#fff',
  },
  image: {
    width: 42,
    height: 42,
    borderColor: 'red',
    resizeMode: 'cover',
    overflow: 'hidden',
    borderRadius: 21,
  },
})
