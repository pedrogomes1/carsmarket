import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    color: '#fff',
  },
  image: {
    width: 60,
    height: 60,
    borderColor: 'red',
    resizeMode: 'cover',
    overflow: 'hidden',
    borderRadius: 30,
  },
})
